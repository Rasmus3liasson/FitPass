// Modular Stripe routes - refactored for better separation of concerns
// Original monolithic file backed up as stripe.ts.backup

import stripeRouter from "./stripe/index";

export default stripeRouter;
